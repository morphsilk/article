@startuml
start

:Аутентификация пользователя;
if (Успешная аутентификация?) then (Да)
  :Загрузка данных пользователя;
else (Нет)
  :Регистрация нового пользователя;
  :Заполнение формы регистрации;
  :Выбор языковых настроек и валюты;
  
endif
:Создание и настройка счетов;
:Добавление счетов с параметрами (название, тип, валюта, баланс);
:Ввод данных о транзакции;
if (Ручное добавление транзакции?) then (Да)
  :Указание категории, суммы и даты;
else (Нет)
  :Получение данных транзакции через API банка;
endif

:Обновление баланса счета;

:Основной функционал (Аналитика, прогнозирование, рекомендации);

:Завершение работы программы;

:Синхронизация данных;

stop
@enduml